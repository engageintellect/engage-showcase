import "./chunk-2GTGKKMZ.js";

// node_modules/.pnpm/@astrojs+svelte@4.0.0_astro@3.0.7_svelte@4.2.0_typescript@5.2.2_vite@4.4.9/node_modules/@astrojs/svelte/client.js
var noop = () => {
};
var originalConsoleWarning;
var consoleFilterRefs = 0;
var client_default = (target) => {
  return (Component, props, slotted, { client }) => {
    if (!target.hasAttribute("ssr"))
      return;
    const slots = {};
    for (const [key, value] of Object.entries(slotted)) {
      slots[key] = createSlotDefinition(key, value);
    }
    try {
      if (import.meta.env.DEV)
        useConsoleFilter();
      const component = new Component({
        target,
        props: {
          ...props,
          $$slots: slots,
          $$scope: { ctx: [] }
        },
        hydrate: client !== "only",
        $$inline: true
      });
      element.addEventListener("astro:unmount", () => component.$destroy(), { once: true });
    } catch (e) {
    } finally {
      if (import.meta.env.DEV)
        finishUsingConsoleFilter();
    }
  };
};
function createSlotDefinition(key, children) {
  let parent;
  return [
    () => ({
      // mount
      m(target) {
        parent = target;
        target.insertAdjacentHTML(
          "beforeend",
          `<astro-slot${key === "default" ? "" : ` name="${key}"`}>${children}</astro-slot>`
        );
      },
      // create
      c: noop,
      // hydrate
      l: noop,
      // destroy
      d() {
        if (!parent)
          return;
        const slot = parent.querySelector(
          `astro-slot${key === "default" ? ":not([name])" : `[name="${key}"]`}`
        );
        if (slot)
          slot.remove();
      }
    }),
    noop,
    noop
  ];
}
function useConsoleFilter() {
  consoleFilterRefs++;
  if (!originalConsoleWarning) {
    originalConsoleWarning = console.warn;
    try {
      console.warn = filteredConsoleWarning;
    } catch (error) {
    }
  }
}
function finishUsingConsoleFilter() {
  consoleFilterRefs--;
}
function filteredConsoleWarning(msg, ...rest) {
  if (consoleFilterRefs > 0 && typeof msg === "string") {
    const isKnownSvelteError = msg.endsWith("was created with unknown prop 'class'");
    if (isKnownSvelteError)
      return;
  }
  originalConsoleWarning(msg, ...rest);
}
export {
  client_default as default
};
//# sourceMappingURL=@astrojs_svelte_client__js.js.map
